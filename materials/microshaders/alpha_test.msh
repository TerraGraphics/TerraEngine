name: "ALPHA_TEST"
group: "ALPHA"
pixel {
    parameters: {
        cbuffers: ["material"]
        includes: ["structures.fxh"]
    }
    source: <<SHADER
void AlphaTest(in float alpha) {
    if (alpha < material.alphaThreshold) {
        discard;
    }
}
SHADER
}
